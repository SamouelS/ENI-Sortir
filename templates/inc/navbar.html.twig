<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="#">ENI-Sortir</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path("home") }}">Accueil</a>
			</li>
			{% if user is defined %}
				{% for role in user.roles %}
					{% if role == "ROLE_ADMIN" %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path("admin_ville") }}">Ville</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path("admin_campus") }}">Campus</a>
						</li>
					{% endif %}
				{% endfor %}
			{% endif %}

			{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("logout") }}">Se déconnecter</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("user_profile", {"id": app.user.id }) }}">Mon profil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("sortie_add") }}">Créer une sortie</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("login") }}">Se connecter</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("user_register") }}">S'inscrire</a>
				</li>
			{% endif %}
			</li>
		</ul>
	</div>
</nav>
